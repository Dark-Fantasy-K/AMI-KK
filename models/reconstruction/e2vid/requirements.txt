# E2VID dependencies — install inside a conda env:
#
#   conda create -n e2vid python=3.10
#   conda activate e2vid
#   conda install pytorch torchvision pytorch-cuda=11.8 -c pytorch -c nvidia
#   pip install -r requirements.txt
#
# (replace pytorch-cuda=11.8 with 12.1 for CUDA 12.x, or omit for CPU-only)

torch>=2.0
torchvision
numpy
opencv-python-headless
h5py
pyyaml
matplotlib
jupyterlab
ipykernel
tqdm
gdown
